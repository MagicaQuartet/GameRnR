const express = require('express');
const router = express.Router();

router.get('/review', );
router.get('/recommendation', );
router.get('/listReview', );
router.get('/listGame', );
router.get('/listRecommendation', );
router.post('/writeReview', );
router.post('/deleteReview', );

module.exports = router;
